[project]
name = "ctr-port-launcher"
version = "0.1.0"
description = "Locked bootstrap environment for the Crash Team Racing port"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false
